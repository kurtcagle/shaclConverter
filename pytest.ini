[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --cov=shacl_transformer
    --cov-report=html
    --cov-report=term-missing
